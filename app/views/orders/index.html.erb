<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Your Orders</title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    />
  </head>
  <body>
    <div class="container mt-5">
      <h1>Your Orders</h1>
      <% if @orders.any? %>
      <table class="table table-striped">
        <thead>
          <tr>
            <th>Chair</th>
            <th>Full Name</th>
            <th>Address</th>
            <th>City</th>
            <th>Postal Code</th>
            <th>Country</th>
            <th>Order Date</th>
          </tr>
        </thead>
        <tbody>
          <% @orders.each do |order| %>
          <tr>
            <td>
              <%= order.chair_id ? Chair.find(order.chair_id).name : "N/A" %>
            </td>
            <td><%= order.full_name %></td>
            <td><%= order.address %></td>
            <td><%= order.city %></td>
            <td><%= order.postal_code %></td>
            <td><%= order.country %></td>
            <td><%= order.created_at.strftime("%B %d, %Y") %></td>
          </tr>
          <% end %>
        </tbody>
      </table>
      <% else %>
      <p>You have no orders yet.</p>
      <% end %>
    </div>
  </body>
</html>
