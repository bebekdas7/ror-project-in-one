<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Chair Details</title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    />
    <style>
      /* styling navbar */
      @import url("https://fonts.googleapis.com/css2?family=Varela+Round&display=swap");
      @import url("https://fonts.googleapis.com/css2?family=Kanit:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&family=Varela+Round&display=swap");
      @import url("https://fonts.googleapis.com/css2?family=Kanit:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&family=Montserrat:ital,wght@0,100..900;1,100..900&family=Varela+Round&display=swap");

      ::-webkit-scrollbar {
        display: none;
      }

      .var {
        font-family: "Varela Round", sans-serif;
        font-weight: 400;
        font-style: normal;
      }
      .kan {
        font-family: "Kanit", sans-serif;
        font-weight: 800;
        font-style: normal;
        font-size: 6rem;
      }

      .primary-btn {
        border: none;
        background: #e9fff0;
        padding: 0.7rem 2rem;
        border-radius: 50px;
      }
      .mons {
        font-family: "Montserrat", sans-serif;
        font-optical-sizing: auto;
      }

      .input {
        border: 1px solid black;
        margin-bottom: 1rem;
        outline: none;
        width: 70%;
        border-radius: 20px;
        height: 2.5rem;
        padding: 0 1rem;
        font-size: 0.9rem;
        color: black;
      }
      .secondary-btn {
        border: none;
        background: #243642;
        color: white;
        width: 70%;
        border-radius: 20px;
        height: 2.5rem;
        margin-top: 2rem;
      }

      .navbar {
        /* border: 1px solid black; */
        height: 10vh;
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: 0 1rem;
        background: #e2f1e7;
      }
      .nav-logo {
        /* border: 1px solid green; */
        width: 10%;
        height: 100%;
      }
      .nav-items {
        /* border: 1px solid orange; */
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 20px;
        width: 45%;
        height: 100%;
      }

      .nav-items a {
        text-decoration: none;
        color: #243642;
        font-size: 1.1rem;
        transition: transform 0.15s ease-in-out;
      }

      .nav-items a:hover {
        transform: scale(1.15);
      }

      /* Basic styling for the body */
      body {
        font-family: Arial, sans-serif;
        background-color: #f4f4f4;
        color: #333;
        margin: 0;
        padding: 20px;
      }

      /* Styling the notice message */
      p {
        font-size: 1rem;
        padding: 0.5rem;
        background-color: #e0f7e0;
        border: 1px solid #c3e6c3;
        border-radius: 5px;
        color: green;
        width: fit-content;
        margin-bottom: 1rem;
      }

      /* Container styling for chair details */
      .chair-container {
        background: #fff;
        border-radius: 10px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        padding: 20px;
        max-width: 800px;
        margin: auto;
        margin-bottom: 1.5rem;
      }

      /* Flex container for links */
      .link-container {
        display: flex;
        gap: 0.5rem;
        margin-top: 1rem;
        align-items: center;
        justify-content: center;
      }

      /* Button styles */
      .btn-group {
        display: flex;
        gap: 1rem;
      }
    </style>
  </head>
  <body>
    <p><%= notice %></p>

    <div class="chair-container">
      <%= render @chair %>
      <% if user_signed_in? %>
        <%= link_to "Buy Now", buy_now_chair_path(@chair), class: "btn btn-success" %>
      <% end %>
    </div>


    <div class="link-container">
      <%= link_to "Edit this chair", edit_chair_path(@chair), class: "btn
      btn-primary" %>
      <%= link_to "Back to chairs", chairs_path, class: "btn
      btn-secondary" %>
      <%= button_to "Destroy this chair", @chair, method:
      :delete, class: "btn btn-danger" %>
      <div class="btn-group"></div>
    </div>
  </body>
</html>
