<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Buy Now - <%= @chair.name %></title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    />
    <style>
      body {
        background-color: #f8f9fa;
        font-family: Arial, sans-serif;
      }

      .container {
        background: #fff;
        border-radius: 10px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        padding: 30px;
        max-width: 800px;
        margin: auto;
      }

      h1 {
        font-size: 2rem;
        color: #343a40;
        margin-bottom: 1.5rem;
      }

      .chair-container {
        background: #e9ecef;
        padding: 20px;
        border-radius: 10px;
        margin-bottom: 20px;
      }

      .form-control {
        border-radius: 5px;
        padding: 10px;
        font-size: 1rem;
      }

      .form-label {
        font-weight: bold;
        margin-bottom: 5px;
      }

      .btn-success {
        background-color: #28a745;
        border: none;
        padding: 10px 20px;
        font-size: 1.1rem;
        border-radius: 5px;
        transition: background-color 0.3s ease;
      }

      .btn-success:hover {
        background-color: #218838;
      }
    </style>
  </head>
  <body>
    <div class="container mt-5">
      <h1>Buy Now - <%= @chair.name %></h1>
      <div class="chair-container mb-4">
        <%= render @chair %>
      </div>

      <h3>Shipping Address</h3>
      <%= form_with model: @order, url: orders_path, method: :post do |form| %>
  <%= form.hidden_field :chair_id, value: @chair.id %>
  <div class="mb-3">
    <%= form.label :full_name, "Full Name" %>
    <%= form.text_field :full_name, class: "form-control" %>
  </div>
  <div class="mb-3">
    <%= form.label :address, "Address" %>
    <%= form.text_area :address, class: "form-control" %>
  </div>
  <div class="mb-3">
    <%= form.label :city, "City" %>
    <%= form.text_field :city, class: "form-control" %>
  </div>
  <div class="mb-3">
    <%= form.label :postal_code, "Postal Code" %>
    <%= form.text_field :postal_code, class: "form-control" %>
  </div>
  <div class="mb-3">
    <%= form.label :country, "Country" %>
    <%= form.text_field :country, class: "form-control" %>
  </div>
  <%= form.submit "Proceed to Checkout", class: "btn btn-success" %>
<% end %>

    </div>
  </body>
</html>
