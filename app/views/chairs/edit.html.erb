<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Editing Chair</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
  <style>
    /* Basic styling for the body */
    body {
      font-family: Arial, sans-serif;
      background-color: #f8f9fa;
      color: #343a40;
      margin: 0;
      padding: 20px;
    }

    /* Container styling for the form */
    .form-container {
      background: #fff;
      border-radius: 10px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      padding: 20px;
      max-width: 600px;
      margin: auto;
    }

    /* Styling the heading */
    h1 {
      text-align: center;
      margin-bottom: 20px;
      color: #007bff;
    }

    /* Styling links */
    a {
      text-decoration: none;
      color: #007bff;
      transition: color 0.3s;
    }

    a:hover {
      color: #0056b3;
    }

    /* Styling the buttons */
    .btn-custom {
      margin: 5px;
    }
  </style>
</head>
<body>
  <% content_for :title, "Editing Chair" %>

  <div class="form-container">
    <h1>Editing Chair</h1>

    <%= render "form", chair: @chair %>

    <br>

    <div class="text-center">
      <%= link_to "Show this chair", @chair, class: "btn btn-outline-primary btn-custom" %>
      <%= link_to "Back to chairs", chairs_path, class: "btn btn-outline-secondary btn-custom" %>
    </div>
  </div>
</body>
</html>
